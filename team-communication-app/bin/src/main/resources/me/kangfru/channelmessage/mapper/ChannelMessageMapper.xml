<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.kangfru.channelmessage.mapper.ChannelMessageMapper">

	<insert id="saveChatToDB">
		INSERT INTO channelmessage(message, sendDate, member_id, channel_id)
		VALUES(#{message}, #{sendDate}, #{member_id}, #{channel_id})
	</insert>
	
</mapper>